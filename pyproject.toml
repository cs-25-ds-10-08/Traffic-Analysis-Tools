[project]
name = "sda"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "darkros", email = "madsmario@gmail.com" }
]
requires-python = ">=3.13"
dependencies = ["pandas", "pandas-stubs"]

[project.scripts]
sda = "sda:main.main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
